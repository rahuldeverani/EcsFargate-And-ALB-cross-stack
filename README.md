# CDK app to create Application Loadbalanced ECS fargate service. ALB is created in one stack and referred into ECS stack.

## Build and deploy:
* npm install
* cdk deploy AlbStack
* cdk deploy Ecsstack
